# Tests

- API unit/integration: `apps/api/tests`
- Future Playwright E2E: this folder (`pnpm exec playwright` against seeded demo)

Phase 2 E2E: login → customer → project → shoot → quote → invoice → client approval.
